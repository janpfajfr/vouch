node_modules/
dist/
.DS_Store

# Private planning — not for the public repo (drafts, strategy, specs, decisions)
.private/

# Claude Code / agent session dumps — never commit these
*-local-command-caveat*.txt
*.tgz
